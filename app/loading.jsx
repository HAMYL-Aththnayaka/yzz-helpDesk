 
 
 export default function Loading(){
    return (
    <main className="text-center">
        <h2 className="text-primary">Loading ...</h2>
        <p>Hint : avoid slow networks ;) </p>
    </main>
    )
 }